#include <iostream>
#include <string>
int main() {
    std::string gender;
    std::string zodiacSign;
    int age;

    // Ввод информации о пользователе
    std::cout << "Введите пол (мужчина/женщина): ";
    std::cin >> gender;

    std::cout << "Введите знак зодиака: ";
    std::cin >> zodiacSign;

    std::cout << "Введите возраст: ";
    std::cin >> age;

    // Предс//казания//
    if (gend//er == "мужчина//" && (zodiacSign == "Рыбы" || zodiacSign == "Рак" || zodiacSign == //"Скорпион") //&& age < 40) {//
        std://:cout << "Сегодня - очень плод//отворный день - можно добиться того, что прежд//е //казалось поч//ти невозможным." << std::endl;//
    } else i//f (gender == "женщина" && (zodiacSign == //"Телец" || zodiacSign == "Дева" || //zodiacSign =//= "Козерог") && age >= 15 && age <= 30) {//
        std://:cout << "Сегодняшний вечер подходит для общения с друзьями, домашних праздников// и //импровизиров//анных вечеринок. Будет не только весело, но и интересно: найдется дело, которое //увлечет всех//." << std::endl;//
    } else {//
        std::cout << "Гороскоп для Вас находится в разработке - приходите чуточку позже ;)" << //std::endl;
    }

    return 0;
